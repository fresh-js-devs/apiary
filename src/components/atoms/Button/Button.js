import styled from "@emotion/styled";

const Button = styled.button({
  fontsize: "13px",
  color: "grey",
  border: "none",
  background: "none",
  textDecoration: "none",
  transition: "0.15s",
  ":hover": {
    fontSize: "13.8px",
    cursor: "pointer",
  },
});

export default Button;
