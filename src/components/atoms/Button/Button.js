import styled from "@emotion/styled";

const Button = styled.button({
  fontsize: "13px",
  color: "grey",
  border: "none",
  background: "none",
  textDecoration: "none",
});

export default Button;
