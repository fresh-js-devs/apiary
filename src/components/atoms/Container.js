import styled from '@emotion/styled';

const Container = styled.div`
  width: 30%;
  padding-top: 200px;
  margin: auto;
  padding: 1rem;
  background-color: #fafafa;
  border: 1px solid #e0e0e0;

  &:hover{
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.16), 0 8px 20px rgba(0, 0, 0, 0.23);
    }
`;

export default Container;