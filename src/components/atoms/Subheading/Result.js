import styled from "@emotion/styled";

const ResultText = styled.p({
  fontSize: "13px",
  color: "grey",
  fontWeight: "bold",
  textAlign: "center",
  marginTop: "13px",
});

export default ResultText;
